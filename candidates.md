# Automation Candidates

Ranked candidates for internal MSP developer tooling, derived from observed Claude Code
session behavior. Generated by `automation-spotter/`; see
`docs/superpowers/specs/2026-08-25-automation-spotter-design.md` for how the data is
produced and redacted.

**`Status` is human-owned.** The spotter updates counts and timings on re-run and never
touches this column. Set it to `building`, `built`, or `rejected` as you triage.

**`Distinct projects` is a count, never engagement names.** No motif below is
client-specific: every row spans two or more distinct engagements, which is what makes it
safe to describe in prose at all.

_Last run: 2026-08-25 · corpus: 2151 steps / 78 sessions / 41 project dirs · 352 motifs found, top 60 emitted_

| ID | Motif | Occurrences | Distinct projects | Median time | Candidate tool | Type | Status |
|---|---|---|---|---|---|---|---|
| `M5D78D` | `Bash(python3) → User` | 22 | 9 | 224s | Long inline-Python run followed immediately by a human correction — one of the highest-friction patterns in the corpus. A structured data-munging helper with a checked contract would remove most of the guesswork that causes the correction. | skill | new |
| `MFF42D` | `Bash(other) → User` | 22 | 11 | 158s | An unrecognized command runs ~2.5min, then the human intervenes. Cannot be named until `BASH_VERBS` covers it — **first action is to widen the allowlist and re-run**, not to build anything. | script | new |
| `M82C6C` | `Bash(python3)` ×169 (cycle) | 169 | 15 | 13s | Repeated consecutive inline-Python invocations — iterating on one-liners for JSON/data inspection. A small query/inspect helper would collapse these runs into one call. | script | new |
| `M12AE1` | `User → User → Bash(other)` | 13 | 12 | 103s | Two consecutive human turns before a long command: the request needed clarifying before work could start. Points at a briefing/scoping step, not a script. | harness | new |
| `M015DF` | `Bash(grep) → Bash(sed)` | 18 | 10 | 4s | Classic search-then-rewrite-in-place loop. A guarded refactor helper (match, preview, apply, verify) would replace it and remove the silent-no-op failure mode `sed` has. | skill | new |
| `M643B3` | `Bash(sed) → Bash(grep)` | 18 | 9 | 7s | The verify half of the same loop — rewrite, then grep to confirm it landed. Same candidate as `M015DF`; listed separately because the ordering shows the verification is manual. | skill | new |
| `M0F498` | `Bash(grep) → Bash(sed) → Bash(sed)` | 11 | 8 | 8s | Longer form of the same motif: one search feeding multiple sequential rewrites. Strengthens the case for a batch-edit primitive over repeated `sed`. | skill | new |
| `MC0B0C` | `ToolSearch → Mcp(linear)` | 17 | 9 | 3s | Linear MCP schemas are fetched on demand before nearly every Linear call. Preloading the common Linear tools in settings would remove a whole round-trip from routine project work. | harness | new |
| `M0B56B` | `User → ToolSearch` | 26 | 13 | 6s | A human request lands and the first action is fetching tool schemas. Same root cause as `MC0B0C` but broader — worth auditing which deferred tools are hot and promoting them. | harness | new |
| `MA2FB7` | `User → Bash(other)` | 45 | 17 | 16s | The most widespread motif in the corpus: a request immediately triggers an unrecognized command. Opaque until the allowlist is widened; tracked so it does not get lost. | script | new |
| `M50CF3` | `User → Bash(find)` | 23 | 15 | 9s | A request lands and the agent starts by hunting the filesystem for orientation. Suggests missing per-repo `CLAUDE.md` structure notes or a shared repo-map skill. | harness | new |
| `MDB4A4` | `User → Skill(other) → User` | 14 | 12 | 4s | A skill is invoked and the human corrects immediately afterward — the skill did not land on first output. Direct feed into the existing skill-tuning loop. | skill | new |
| `M1E83B` | `Bash(other) → Bash(python3)` | 24 | 13 | 22s | An unrecognized command followed by inline Python — likely running something, then parsing its output by hand. A structured-output wrapper would join the two. | script | new |
| `M4EB84` | `Bash(other)` ×55 (cycle) | 55 | 11 | 11s | Repeated consecutive runs of the same unrecognized command — retry-until-it-works on a custom script. High-value once the allowlist can name it. | script | new |
| `MC690C` | `Bash(find) → Bash(other) → Bash(other)` | 10 | 9 | 22s | Locate a file, then act on it twice with custom commands. A find-and-operate wrapper is plausible but this needs the allowlist widened before it can be specified. | script | new |
| `M3A75D` | `Bash(sed) → Bash(sed) → User` | 8 | 5 | 316s | Two in-place rewrites, then a ~5min human correction. The worst-outcome version of the grep/sed loop above: the edits land, nobody verifies, and the human catches it later. A guarded edit primitive that previews and verifies would kill this and `M56043` together. | skill | new |
| `M56043` | `Bash(sed) → User` | 10 | 6 | 315s | Single in-place rewrite followed by a ~5min human correction, across 6 engagements. Tied with `M3A75D` for the highest median elapsed in the corpus — the strongest ROI signal available. | skill | new |
| `M039B6` | `Bash(python3) → Bash(python3) → User` | 8 | 5 | 259s | Two inline-Python runs, then a correction. Deeper form of `M5D78D`; confirms the friction is the ad-hoc Python itself, not one unlucky invocation. | skill | new |
| `M063E8` | `User → User → Bash(other) → Bash(other)` | 6 | 5 | 244s | Two human turns to get the ask right, then two long unrecognized commands. The clarification half is the same signal as `M12AE1`; the command half is opaque until the allowlist widens. | harness | new |
| `M01E0B` | `Bash(other) → Bash(other) → User` | 7 | 6 | 199s | Two unrecognized/custom commands run back to back, then the human intervenes. Same family as `MFF42D` — blocked on naming the command before a tool can be specified. | script | new |
| `M87831` | `Bash(grep) → User` | 9 | 7 | 189s | A search runs and the human corrects ~3min later — the agent looked in the wrong place. Points at missing repo orientation (structure notes, a repo map) rather than a better search tool. | harness | new |
| `M7E0F0` | `User → User → ToolSearch` | 7 | 7 | 114s | Clarification turn, then a deferred-tool schema fetch. Combines the two harness signals already tracked (`M12AE1`, `M0B56B`): the ask needed sharpening *and* the tools were not preloaded. | harness | new |
| `M38DAB` | `User → Bash(python3) ×3` | 9 | 5 | 63s | One request expanding into a three-deep chain of inline-Python runs. Quantifies the depth behind the `M82C6C` cycle — a single request routinely costs three hand-written one-liners. | script | new |
| `M109FD` | `Bash(other) → Bash(sed)` | 17 | 10 | 15s | An unrecognized command feeding an in-place rewrite, across 10 engagements. The rewrite half is covered by the batch-edit candidate; the input half needs the allowlist. | script | new |
| `M51424` | `Bash(sed)` ×32 (cycle) | 32 | 8 | 4s | Repeated consecutive in-place rewrites — the same file edited over and over one expression at a time. The clearest single argument for a batch-edit primitive (`M015DF`, `M643B3`, `M0F498`, `M3A75D`, `M56043` are all the same underlying gap). | skill | new |
| `M481EC` | `Mcp(harvest)` ×56 (cycle) | 56 | 3 | 4s | 56 consecutive Harvest MCP calls — bulk time-tracking work done one entry at a time. A batch time-entry operation (or a local CSV-to-Harvest loader) would collapse a 4min click-through into one call. | mcp | new |
| `M99610` | `Mcp(linear)` ×37 (cycle) | 37 | 4 | 3s | 37 consecutive Linear MCP calls — ticket creation/update driven one API call per ticket. A bulk-upsert path would pair well with the existing Linear project-management skills. | mcp | new |
| `M0AEFD` | `User → Bash(git)` | 14 | 12 | 12s | A request lands and the first move is a git call for orientation (status/log/diff). Widest-spread orientation motif after `M50CF3`; a session-start context primer would remove both. | harness | new |
| `MFCE6E` | `Bash(find) → Bash(grep)` | 9 | 8 | 7s | Locate candidate files, then search inside them. Third variant of the orientation pattern — same candidate as `M50CF3` and `M0AEFD`, more evidence. | harness | new |
