# Automation Candidates

Ranked candidates for internal MSP developer tooling, derived from observed Claude Code
session behavior. Generated by `comb/`; see
`docs/superpowers/specs/2026-08-25-comb-design.md` for how the data is
produced and redacted.

**`Status` is human-owned.** comb updates counts and timings on re-run and never
touches this column. Set it to `building`, `built`, or `rejected` as you triage.

**`Distinct projects` is a count, never engagement names.** No motif below is
client-specific: every row spans two or more distinct engagements, which is what makes it
safe to describe in prose at all.

_Last run: 2026-09-10 · corpus: 8975 steps / 194 sessions / 65 project dirs · 1998 motifs found, top 60 emitted_

| ID | Motif | Occurrences | Distinct projects | Median time | Candidate tool | Type | Status |
|---|---|---|---|---|---|---|---|
| `M5D78D` | `Bash(python3) → User` | 54 | 19 | 183s | Long inline-Python run followed immediately by a human correction — one of the highest-friction patterns in the corpus. A structured data-munging helper with a checked contract would remove most of the guesswork that causes the correction. | skill | new |
| `MFF42D` | `Bash(other) → User` | 113 | 26 | 183s | A path-invoked command runs ~3min, then the human intervenes. Widening `BASH_VERBS` will NOT name it: all 12 candidate runner verbs are already listed, and `pytest`/`npm`/`npx`/`just` score 0 steps. `projectBash` fails closed on any path-like head, so repo scripts and unknown verbs share this token. **First action is the `Bash(script)` split plus an internal-script allowlist** (see `docs/superpowers/specs/2026-09-10-comb-tools-docs-plugins.md`). | script | new |
| `M82C6C` | `Bash(python3)` ×319 (cycle) | 319 | 23 | 14s | Repeated consecutive inline-Python invocations — iterating on one-liners for JSON/data inspection. A small query/inspect helper would collapse these runs into one call. | script | new |
| `M12AE1` | `User → User → Bash(other)` | 34 | 18 | 96s | Two consecutive human turns before a long command: the request needed clarifying before work could start. Points at a briefing/scoping step, not a script. | harness | new |
| `M015DF` | `Bash(grep) → Bash(sed)` | 58 | 23 | 5s | Classic search-then-rewrite-in-place loop. A guarded refactor helper (match, preview, apply, verify) would replace it and remove the silent-no-op failure mode `sed` has. | skill | new |
| `M643B3` | `Bash(sed) → Bash(grep)` | 48 | 20 | 7s | The verify half of the same loop — rewrite, then grep to confirm it landed. Same candidate as `M015DF`; listed separately because the ordering shows the verification is manual. | skill | new |
| `M0F498` | `Bash(grep) → Bash(sed) → Bash(sed)` | 20 | 15 | 17s | Longer form of the same motif: one search feeding multiple sequential rewrites. Strengthens the case for a batch-edit primitive over repeated `sed`. | skill | new |
| `MC0B0C` | `ToolSearch → Mcp(linear)` | 100 | 26 | 4s | Linear MCP schemas are fetched on demand before nearly every Linear call. Preloading the common Linear tools in settings would remove a whole round-trip from routine project work. | harness | new |
| `M0B56B` | `User → ToolSearch` | 133 | 32 | 8s | A human request lands and the first action is fetching tool schemas. Same root cause as `MC0B0C` but broader — worth auditing which deferred tools are hot and promoting them. | harness | new |
| `MA2FB7` | `User → Bash(other)` | 149 | 32 | 18s | The most widespread motif in the corpus: a request immediately triggers an unrecognized command. Opaque until the allowlist is widened; tracked so it does not get lost. | script | new |
| `M50CF3` | `User → Bash(find)` | 48 | 28 | 10s | A request lands and the agent starts by hunting the filesystem for orientation. Suggests missing per-repo `CLAUDE.md` structure notes or a shared repo-map skill. | harness | new |
| `MDB4A4` | `User → Skill(other) → User` | 37 | 24 | 7s | A skill is invoked and the human corrects immediately afterward — the skill did not land on first output. Direct feed into the existing skill-tuning loop. | skill | new |
| `M1E83B` | `Bash(other) → Bash(python3)` | 58 | 21 | 21s | An unrecognized command followed by inline Python — likely running something, then parsing its output by hand. A structured-output wrapper would join the two. | script | new |
| `M4EB84` | `Bash(other)` ×326 (cycle) | 326 | 22 | 16s | Repeated consecutive runs of the same path-invoked command: retry-until-it-works on a repo script. 311 such streaks exist corpus-wide. High-value once the internal-script allowlist can name which script, which is not a `BASH_VERBS` change. | script | new |
| `MC690C` | `Bash(find) → Bash(other) → Bash(other)` | 11 | 10 | 25s | Locate a file, then act on it twice with custom commands. A find-and-operate wrapper is plausible but this needs the allowlist widened before it can be specified. | script | new |
| `M3A75D` | `Bash(sed) → Bash(sed) → User` | 8 | 5 | 316s | Two in-place rewrites, then a ~5min human correction. The worst-outcome version of the grep/sed loop above: the edits land, nobody verifies, and the human catches it later. A guarded edit primitive that previews and verifies would kill this and `M56043` together. | skill | new |
| `M56043` | `Bash(sed) → User` | 13 | 9 | 294s | Single in-place rewrite followed by a ~5min human correction, across 6 engagements. Tied with `M3A75D` for the highest median elapsed in the corpus — the strongest ROI signal available. | skill | new |
| `M039B6` | `Bash(python3) → Bash(python3) → User` | 8 | 5 | 259s | Two inline-Python runs, then a correction. Deeper form of `M5D78D`; confirms the friction is the ad-hoc Python itself, not one unlucky invocation. | skill | new |
| `M063E8` | `User → User → Bash(other) → Bash(other)` | 7 | 6 | 248s | Two human turns to get the ask right, then two long unrecognized commands. The clarification half is the same signal as `M12AE1`; the command half is opaque until the allowlist widens. | harness | new |
| `M01E0B` | `Bash(other) → Bash(other) → User` | 29 | 13 | 208s | Two path-invoked commands run back to back, then the human intervenes. Same family as `MFF42D`, and blocked on the same projection change, not on a wider verb allowlist. | script | new |
| `M87831` | `Bash(grep) → User` | 26 | 17 | 203s | A search runs and the human corrects ~3min later — the agent looked in the wrong place. Points at missing repo orientation (structure notes, a repo map) rather than a better search tool. | harness | new |
| `M7E0F0` | `User → User → ToolSearch` | 20 | 15 | 79s | Clarification turn, then a deferred-tool schema fetch. Combines the two harness signals already tracked (`M12AE1`, `M0B56B`): the ask needed sharpening *and* the tools were not preloaded. | harness | new |
| `M38DAB` | `User → Bash(python3) → Bash(python3) → Bash(python3)` | 22 | 11 | 61s | One request expanding into a three-deep chain of inline-Python runs. Quantifies the depth behind the `M82C6C` cycle — a single request routinely costs three hand-written one-liners. | script | new |
| `M109FD` | `Bash(other) → Bash(sed)` | 48 | 22 | 9s | An unrecognized command feeding an in-place rewrite, across 10 engagements. The rewrite half is covered by the batch-edit candidate; the input half needs the allowlist. | script | new |
| `M51424` | `Bash(sed)` ×96 (cycle) | 96 | 15 | 7s | Repeated consecutive in-place rewrites — the same file edited over and over one expression at a time. The clearest single argument for a batch-edit primitive (`M015DF`, `M643B3`, `M0F498`, `M3A75D`, `M56043` are all the same underlying gap). | skill | new |
| `M481EC` | `Mcp(harvest)` ×70 (cycle) | 70 | 3 | 4s | 70 consecutive Harvest MCP calls — bulk time-tracking work done one entry at a time. A batch time-entry operation (or a local CSV-to-Harvest loader) would collapse a 4min click-through into one call. | mcp | new |
| `M99610` | `Mcp(linear)` ×320 (cycle) | 320 | 23 | 3s | 65 consecutive Linear MCP calls — ticket creation/update driven one API call per ticket. A bulk-upsert path would pair well with the existing Linear project-management skills. | mcp | new |
| `M0AEFD` | `User → Bash(git)` | 96 | 33 | 10s | A request lands and the first move is a git call for orientation (status/log/diff). Widest-spread orientation motif after `M50CF3`; a session-start context primer would remove both. | harness | new |
| `MFCE6E` | `Bash(find) → Bash(grep)` | 9 | 8 | 7s | Locate candidate files, then search inside them. Third variant of the orientation pattern — same candidate as `M50CF3` and `M0AEFD`, more evidence. | harness | new |
| `M78DE2` | `Mcp(linear) → User` | 68 | 20 | 347s | A Linear MCP call, then a ~6min human correction — the longest median in the whole corpus. Tickets are being written through the API and then reworked by hand, which says the ticket *content* (sizing, acceptance criteria, wording) is what needs a house style, not the API path. Feeds the existing Linear project-management skills. | skill | new |
| `M27C5A` | `Bash(git) → User` | 67 | 22 | 189s | A git call followed by a ~4min human correction, across 8 engagements. Pairs with the `M5FBC4` cycle: git state is being read or written and then disputed. Likely a commit/branch/PR convention that should be enforced by a hook rather than remembered. | hook | new |
| `M5FBC4` | `Bash(git)` ×109 (cycle) | 109 | 10 | 8s | Repeated consecutive git calls — status, log, diff, add, commit walked one command at a time. The cycle half of `M27C5A`; a single scripted commit-and-verify path would collapse it. | script | new |
| `M6E396` | `Task(general-purpose) → User` | 66 | 12 | 169s | A general-purpose subagent returns and the human intervenes ~2min later, 33 times. Delegated work is not landing on first output — the fix is a tighter brief/return contract for dispatched agents, not a new tool. | harness | new |
| `M0C195` | `User → Task(general-purpose)` | 45 | 11 | 47s | A request lands and is immediately delegated to a general-purpose subagent, 23 times across 4 engagements. Paired with `M6E396` this is a delegation loop with no specialised agent behind it — a named subagent type with a real prompt would replace the ad-hoc dispatch. | harness | new |
| `ME733C` | `Bash(python3) → User → User` | 10 | 8 | 312s | Inline Python runs, then two consecutive human turns over ~5min. The deepest form of `M5D78D`: the ad-hoc Python was wrong enough to need a conversation, not a one-line correction. Strongest evidence yet for a checked data-munging helper. | skill | new |
| `M7A5A3` | `User → Bash(python3)` | 72 | 19 | 25s | A request lands and the first action is a hand-written inline-Python one-liner, 32 times across 12 engagements. The entry point to the whole `M82C6C`/`M38DAB` chain — quantifies how routinely ad-hoc Python is the default first move. | script | new |
| `M45638` | `Skill(other) → User` | 51 | 28 | 0s | NOT a correction. The 19ms median is below human reaction time: a `Skill` call is followed by a same-instant `user` record because the skill body is delivered as a user-role message, and `projectLines` counts any non-tool-result user record as `User`. Treat as a measurement artifact until `correctionRate` carries a latency floor. Do not build against this row. | skill | new |
| `M3BE62` | `ToolSearch → Mcp(fireflies)` | 12 | 7 | 4s | Deferred-tool schemas fetched on demand before a Fireflies call. Same gap as `MC0B0C` but a different MCP, which upgrades that candidate: the fix is preloading whichever deferred tools are hot per project, not special-casing Linear. | harness | new |
| `M147BC` | `User → Bash(other) → User` | 37 | 11 | 164s | A request, one unrecognized/custom command running ~2min, then a human turn. Correction sandwich; blocked on `BASH_VERBS` before the command can be named or a tool specified. | script | new |
| `M829AC` | `ToolSearch → Mcp(linear) → Mcp(linear)` | 66 | 20 | 8s | Schema fetch, then two Linear calls in a row. Representative of a family of ToolSearch → Mcp(linear)×N chains (up to six calls deep) that all share the root cause of `MC0B0C`: Linear schemas are deferred and fetched per session. Preload them. | harness | new |
| `M70EED` | `Bash(other) → Bash(git)` | 66 | 21 | 12s | An unrecognized or custom command immediately followed by a git call, across 21 engagements. Likely a build or generate step then commit. A commit-after-generate wrapper is the candidate once the allowlist names the command. | script | new |
| `MD8F0A` | `Bash(git) → Bash(other)` | 54 | 21 | 14s | A git call followed by an unrecognized or custom command, across 21 engagements. The mirror of `M70EED`: checkout or pull, then run something. Same candidate once the command is named. | script | new |
| `M15A63` | `Bash(python3) → Bash(other)` | 68 | 20 | 21s | Inline Python followed by an unrecognized or custom command, across 19 engagements. Data prep feeding a custom tool. Pairs with `M1E83B`, which is the reverse order. | script | new |
| `MEC893` | `Bash(sed) → Bash(python3)` | 62 | 17 | 14s | An in-place rewrite followed by an inline Python check, across 17 engagements. Edit then verify by script. A rewrite helper that runs a verification hook would fold both steps. | skill | new |
| `M9ED40` | `Mcp(box)` ×164 (cycle) | 164 | 11 | 4s | 164 consecutive Box MCP calls across 11 engagements, median 4s each. Bulk document work driven one API call per file. A batch Box list-and-fetch helper is the candidate. | mcp | new |
| `M625A8` | `User → Skill(other)` | 40 | 25 | 7s | A human request immediately dispatches a non-generic skill, across 24 engagements. Skills are the first move often enough that skill discovery and argument shaping deserve a look. Pairs with `MDB4A4`, where the human corrects right after. | harness | new |
| `M0A9EB` | `User → Bash(other) → Bash(other)` | 33 | 16 | 39s | A request, then two unrecognized or custom commands in a row, across 16 engagements. The two-step form of `MA2FB7`. Cannot be named until `BASH_VERBS` covers the commands. | script | new |
| `MD2451` | `User → Bash(python3) → Bash(python3)` | 34 | 14 | 45s | A request expanding into two inline Python runs, median 45s, across 14 engagements. The two-deep form of `M7A5A3` and the shallow form of `M38DAB`. Same data-inspection helper candidate. | script | new |
| `M8E6CC` | `Bash(other) → User → Bash(other)` | 35 | 13 | 212s | Custom command, human turn, same or another custom command, median 4min, across 13 engagements. A correction sandwich: the first attempt missed and was re-run after human input. High friction per occurrence. | script | new |
| `M0D445` | `Mcp(linear) → Mcp(linear) → User` | 32 | 14 | 410s | Two Linear calls then a human turn at a 7min median, across 14 engagements. Extends `M78DE2`: ticket writes are being reviewed and corrected by hand. A ticket preview-before-write step is the candidate. | skill | new |
| `MAA975` | `Mcp(linear) → Mcp(linear) → Mcp(linear) → Mcp(linear) → User` | 16 | 10 | 605s | Four consecutive Linear calls then a human turn at a 10min median, across 10 engagements. The longest median in the corpus. Same family as `M0D445` and `M78DE2`: bulk ticket edits reviewed after the fact. | skill | new |
| `M1ADAB` | `User → Bash(sed)` | 43 | 17 | 11s | A request answered with an immediate in-place rewrite, across 16 engagements. The agent knows the edit before looking. Fine when right; pairs with `M56043` when the human corrects a sed afterward. | harness | new |
| `MAE684` | `User → Bash(gh)` | 34 | 14 | 8s | A request answered with a GitHub CLI call, across 13 engagements. PR and issue lookups as the first move. A PR-context summarizer that runs at session start would pre-empt it. | skill | new |
| `M5C007` | `User → ToolSearch → Mcp(linear)` | 53 | 19 | 12s | A request lands, tool schemas are fetched, then a Linear call — the entry point of the ToolSearch→Linear family already tracked as `MC0B0C`/`M829AC`. Preload the hot Linear tools. | harness | new |
| `M0CF4B` | `ToolSearch → Mcp(linear) → Mcp(linear) → Mcp(linear)` | 38 | 19 | 13s | Schema fetch followed by three consecutive Linear calls. Depth evidence for the same preload fix; each schema round-trip is amortised over several calls, so the fetch is pure overhead. | harness | new |
| `M820B0` | `ToolSearch → Mcp(linear) → Mcp(linear) → Mcp(linear) → Mcp(linear)` | 29 | 17 | 16s | Schema fetch followed by four consecutive Linear calls. Same root cause as `M0CF4B`, one call deeper. | harness | new |
| `M3CD5D` | `User → ToolSearch → Mcp(linear) → Mcp(linear)` | 36 | 14 | 13s | Request, schema fetch, two Linear calls. The two-deep form of `M5C007`. | harness | new |
| `M61AC7` | `ToolSearch → Mcp(linear) → Mcp(linear) → Mcp(linear) → Mcp(linear) → Mcp(linear)` | 19 | 14 | 19s | Schema fetch followed by five consecutive Linear calls — the deepest chain in this family. Confirms the schema fetch is a fixed per-session cost, not a per-call one. | harness | new |
| `MAFD79` | `Bash(sed) → Bash(other)` | 38 | 19 | 16s | An in-place rewrite followed by an unrecognized or custom command, across 19 engagements. The mirror of `M109FD`: edit, then run something over the result. Same batch-edit candidate; the command half needs the allowlist. | script | new |
| `MBE36F` | `Bash(sed) → Bash(python3) → Bash(python3)` | 30 | 15 | 41s | An in-place rewrite then two inline-Python runs, median 41s, across 15 engagements. Deeper form of `MEC893` — the verification after a `sed` costs two hand-written scripts, not one. | skill | new |
| `ME3F08` | `Bash(other) → Bash(grep)` | 33 | 20 | 13s | An unrecognized or custom command followed by a search, across 20 engagements. Run something, then grep its output or the files it touched. A structured-output wrapper would remove the search. | script | new |
| `MDA856` | `Bash(other) → Bash(other) → Bash(git)` | 26 | 14 | 32s | Two unrecognized or custom commands then a git call, median 32s, across 14 engagements. The three-step form of `M70EED`: generate or build twice, then commit. | script | new |
| `M7180B` | `Bash(grep) → Bash(other)` | 30 | 18 | 17s | A search followed by an unrecognized or custom command, across 18 engagements. The reverse of `ME3F08`. Locate, then act with a custom tool. | script | new |
| `ME633B` | `User → ToolSearch → Mcp(linear) → Mcp(linear) → Mcp(linear)` | 20 | 10 | 18s | Request, schema fetch, three Linear calls. Same family as `M5C007`, one call deeper. | harness | new |
| `M1EEA9` | `User → Bash(grep)` | 29 | 17 | 18s | A request answered with an immediate search, across 17 engagements. Orientation motif in the same family as `M50CF3` (`find`) and `M0AEFD` (`git`) — repo structure notes would pre-empt all three. | harness | new |
| `MBEB86` | `Bash(sed) → Bash(grep) → Bash(sed)` | 21 | 15 | 14s | Rewrite, verify by search, rewrite again — median 14s, across 15 engagements. The explicit edit-verify-edit loop behind `M015DF`/`M643B3`. A guarded batch-edit primitive with built-in verification replaces the whole loop. | skill | new |
| `M36122` | `Bash(python3) → Bash(grep)` | 36 | 13 | 12s | Inline Python then a search, across 13 engagements. Parse something by hand, then grep the result. | script | new |
| `M97081` | `Bash(grep) → Bash(python3)` | 31 | 14 | 18s | A search then inline Python, across 14 engagements. The reverse of `M36122`: locate, then hand-parse. | script | new |
| `MA91B4` | `User → ToolSearch → Mcp(linear) → Mcp(linear) → Mcp(linear) → Mcp(linear)` | 16 | 9 | 20s | Request, schema fetch, four Linear calls. Deepest request-rooted form of the Linear preload family. | harness | new |
| `MA9006` | `User → Bash(git) → Bash(git)` | 27 | 10 | 21s | A request answered with two consecutive git calls, median 21s, across 10 engagements. The two-deep form of `M0AEFD`; a session-start context primer would cover it. | harness | new |
